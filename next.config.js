/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}
// next.config.js

module.exports = {
  images: {
    domains: [], // Порожній масив дозволить використовувати зображення з будь-якого хоста
  },
};


module.exports = nextConfig
